# Installed by froggle-ws (diverts the distro copy). Used by plain GTK 4 apps
# (built-in theme, dark variant); libadwaita apps follow
# org.gnome.desktop.interface color-scheme instead.
[Settings]
gtk-application-prefer-dark-theme=true
