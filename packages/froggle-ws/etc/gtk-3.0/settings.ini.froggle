# Installed by froggle-ws (diverts the distro copy). Used by GTK 3 apps when no
# settings daemon overrides it: GNOME uses gsettings, Xfce uses xsettings.xml.
[Settings]
gtk-theme-name=Adwaita-dark
gtk-application-prefer-dark-theme=true
